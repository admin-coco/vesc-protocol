[build]
builder = "nixpacks"
buildCommand = "cd oracle-v2 && npm install"

[deploy]
startCommand = "node oracle-v2/rate-updater.js --watch"
restartPolicyType = "on_failure"

[nixpacks]
pkgs = ["nodejs_20"]
